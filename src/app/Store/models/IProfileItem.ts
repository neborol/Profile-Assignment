// Signature of the Profile Item as determined by this interface.
export interface IProfileItem {
    firstName: string;
    lastName: string;
    gender: string;
    role: string;
    showDetails: boolean;
}
